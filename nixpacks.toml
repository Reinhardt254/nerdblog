[phases.setup]
nixPkgs = ["php84", "php84Extensions.mbstring", "php84Extensions.xml", "php84Extensions.curl", "php84Extensions.zip", "php84Extensions.pdo_mysql", "php84Extensions.gd"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
