[phases.setup]
nixPkgs = ["php84", "php84Extensions.mbstring", "php84Extensions.xml", "php84Extensions.curl", "php84Extensions.zip", "php84Extensions.gd", "php84Extensions.pdo", "php84Packages.composer"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-dev", "npm install"]

[phases.build]
cmds = [
    "npm run production",
    "mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache",
    "chmod -R a+rw storage",
    "touch database/database.sqlite",
    "php artisan migrate --force",
    "php artisan storage:link",
    "cd vendor/statamic/cms && npm install && npm run build && cd /app",
    "php artisan statamic:install",
    "php artisan statamic:assets:generate-presets",
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan view:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8000}"
